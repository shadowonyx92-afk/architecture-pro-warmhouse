# Project_template

# Задание 1. Анализ и планирование

### 1. Описание функциональности монолитного приложения

#### **Управление отоплением:**

**Пользователи могут:**
- регистрировать устройства в системе - **POST /api/v1/sensors**
- просматривать список устройств - **GET /api/v1/sensors**
- получать информацию о конкретном устройстве - **GET /api/v1/sensors/:id**
- изменять параметры устройства (включение/выключение) - **PATCH /api/v1/sensors/:id/value**
- удалять устройства - **DELETE /api/v1/sensors/:id**

**Система поддерживает:**
- хранение устройств в базе данных в таблице sensors
- централизованное управление через сервер - *h.DB.UpdateSensorValue(...)*
- синхронное управление устройствами

#### **Мониторинг температуры:**

**Пользователи могут:**
- просматривать текущую температуру - **GET /api/v1/sensors**
- получать температуру по локации - **GET /api/v1/sensors/temperature/:location**
- получать температуру конкретного датчика - **GET /api/v1/sensors/:id**

**Система поддерживает:**
- температурные датчики - *Temperature SensorType = "temperature"*
- получение данных в реальном времени - *s.HTTPClient.Get(fmt.Sprintf("%s/temperature/%s", s.BaseURL, sensorID))*

### 2. Анализ архитектуры монолитного приложения

**Технологический стек:**
- Язык программирования: Go
- Web-фреймворк: Gin
- База данных: PostgreSQL
- БД-драйвер: pgx
- Контейнеризация: Docker + Docker Compose

**Структура приложения:**
- Монолитная, все компоненты системы (обработка запросов, бизнес-логика, работа с данными) находятся в рамках одного приложения
- Отсутствует чёткое разделение доменных правил и инфраструктурного кода
- Все запросы обрабатываются синхронно
- Бизнес логика разделена в обработчике (handlers) и в сервисах 
- Архитектура основана на модели request–response
- PostgreSQL используется как единый источник данных

**Минусы:**
Описаны в блоке **4. Проблемы монолитного решения**

**Плюсы:**
- Простота реализации и понимания
- Минимальные операционные затраты
- Быстрый старт для MVP
- Отсутствие сетевой сложности между внутренними компонентами

### 3. Определение доменов и границы контекстов

У нас получается три основных контекста:
- Управление устройствами (Device Manager) - основная суть хранить и управлять датчиками умного дома
- Мониторинг температуры (Temperature monitoring) - получает актуальные данные температуры
- Интеграция со всеми системи (External Systems) - всеообщее взаимодействие системы

**Управление устройствами:**
Доменная модель **Sensor**:
- **ID** - уникальный идентификатор устройства
- **Name** - имя устройства
- **Type** - тип датчика
- **Location** - физическое расположение датчика в доме
- **Status** - состояния датчика (ВКЛ/ВЫКЛ)
- **Value** - значение датчика
- **Unit** - единица измерения
- **LastUpdate** - последняя дата обновления
- **CreateAt** - время создания записи

**Мониторинг температуры:**
Доменная модель **TemperatureResponse**:
- **Value** - текущее значение температуры
- **Unit** - единица измерения
- **Timestamp** - время измерения температуры
- **Location** - место измерения температуры
- **Descriptio** - дополнительная информация
- **Status** - состояние датчика
- **SensorID** - уникальный идентификатор устройства
- **SensorType** - тип датчика

**Интеграция со всеми системи:**
Не содержит доменные сущности, а лишь интерфейсы и протоколы обмена данными

Если обратить внимание, то имена некоторых полей повторяются:
- Unit и Type — совпадают по смыслу и одинаковые для доменов
- Location, Value, Status — совпадают по имени, но смысл другой, поэтому при разделении монолита на сервисы нужно их держать отдельно

### **4. Проблемы монолитного решения**

- Отсутствует логирование данных
- Отсутствует история изменений данных
- Обновление или изменение функциональности требует пересборки образа и перезапуска всего приложения
- Масштабирование возможно только целиком, без выделения отдельных частей
- Домены живут в одном процессе
- Одна БД используется для всего

На самом деле решение нормальное, простое MVP решение для быстрого старта, чтобы было что показать как прототип или на каком нибудь демо.

### 5. Визуализация контекста системы — диаграмма С4

[Схема контекста](./schemas/context.puml)

# Задание 2. Проектирование микросервисной архитектуры

В этом задании вам нужно предоставить только диаграммы в модели C4. Мы не просим вас отдельно описывать получившиеся микросервисы и то, как вы определили взаимодействия между компонентами To-Be системы. Если вы правильно подготовите диаграммы C4, они и так это покажут.

**Диаграмма контейнеров (Containers)**

Добавьте диаграмму.

**Диаграмма компонентов (Components)**

Добавьте диаграмму для каждого из выделенных микросервисов.

**Диаграмма кода (Code)**

Добавьте одну диаграмму или несколько.

# Задание 3. Разработка ER-диаграммы

Добавьте сюда ER-диаграмму. Она должна отражать ключевые сущности системы, их атрибуты и тип связей между ними.

# Задание 4. Создание и документирование API

### 1. Тип API

Укажите, какой тип API вы будете использовать для взаимодействия микросервисов. Объясните своё решение.

### 2. Документация API

Здесь приложите ссылки на документацию API для микросервисов, которые вы спроектировали в первой части проектной работы. Для документирования используйте Swagger/OpenAPI или AsyncAPI.

# Задание 5. Работа с docker и docker-compose

Перейдите в apps.

Там находится приложение-монолит для работы с датчиками температуры. В README.md описано как запустить решение.

Вам нужно:

1) сделать простое приложение temperature-api на любом удобном для вас языке программирования, которое при запросе /temperature?location= будет отдавать рандомное значение температуры.

Locations - название комнаты, sensorId - идентификатор названия комнаты

```
	// If no location is provided, use a default based on sensor ID
	if location == "" {
		switch sensorID {
		case "1":
			location = "Living Room"
		case "2":
			location = "Bedroom"
		case "3":
			location = "Kitchen"
		default:
			location = "Unknown"
		}
	}

	// If no sensor ID is provided, generate one based on location
	if sensorID == "" {
		switch location {
		case "Living Room":
			sensorID = "1"
		case "Bedroom":
			sensorID = "2"
		case "Kitchen":
			sensorID = "3"
		default:
			sensorID = "0"
		}
	}
```

2) Приложение следует упаковать в Docker и добавить в docker-compose. Порт по умолчанию должен быть 8081

3) Кроме того для smart_home приложения требуется база данных - добавьте в docker-compose файл настройки для запуска postgres с указанием скрипта инициализации ./smart_home/init.sql

Для проверки можно использовать Postman коллекцию smarthome-api.postman_collection.json и вызвать:

- Create Sensor
- Get All Sensors

Должно при каждом вызове отображаться разное значение температуры

Ревьюер будет проверять точно так же.


# **Задание 6. Разработка MVP**

Необходимо создать новые микросервисы и обеспечить их интеграции с существующим монолитом для плавного перехода к микросервисной архитектуре. 

### **Что нужно сделать**

1. Создайте новые микросервисы для управления телеметрией и устройствами (с простейшей логикой), которые будут интегрированы с существующим монолитным приложением. Каждый микросервис на своем ООП языке.
2. Обеспечьте взаимодействие между микросервисами и монолитом (при желании с помощью брокера сообщений), чтобы постепенно перенести функциональность из монолита в микросервисы. 

В результате у вас должны быть созданы Dockerfiles и docker-compose для запуска микросервисов. 